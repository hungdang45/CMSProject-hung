@model IEnumerable<CMSProject.Models.Category>


@{

    Layout = "~/Views/Shared/admin1.cshtml";
}


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<a href="/tao-moi-danh-muc"  class="elements">Tạo mới danh mục</a>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Desciption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SubCategory)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desciption)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubCategory)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.CategoryID }) |*@
                <a href="/Categories/Edit/@item.CategoryID" class="btn btn-danger">Chỉnh sửa</a>|
                <a href="/chi-tiet-danh-muc/@item.CategoryID" )" class="elements">Chi tiết</a>|
                @*@Html.ActionLink("Details", "Details", new { id = item.CategoryID }) |*@
                @Html.ActionLink("Delete", "Delete", new { id = item.CategoryID })
            </td>
        </tr>
    }

</table>
